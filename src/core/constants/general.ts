export const GENERIC_ERROR_MESSAGE = "Something went wrong. Please try again later.";
export const DEBOUNCE_DELAY = 700;
/** How often the feed re-reads the page on screen, so likes and reply counts stay live. */
export const FEED_POLL_INTERVAL = 30_000;
/** Faster cadence while a video on screen is still transcoding — the user is waiting on that one. */
export const PROCESSING_POLL_INTERVAL = 10_000;
// Polling interval (in milliseconds)
export const POLLING_INTERVAL = 60000;

export const RATIO_4_3 = 4 / 3;
export const RATIO_16_9 = 16 / 9;
